/* eslint-disable no-unused-vars */
/** contains refs to vendor libs. Use by webpack in prod mode only. vendor libs should change
 *  less frequently than main.
 */

 import fetch from 'whatwg-fetch';
